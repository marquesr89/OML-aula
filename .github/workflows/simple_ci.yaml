name: Simple CI/CD

on:
  push:

jobs:
  simpleci:
    runs-on: ubuntu-latest
    steps:
      - name: listar diretoria
        run: dir
      - name: listar caminho
        run: cd
      - name: Checkout
        uses: actions/Checkout@v3
      - name: listar diretoria
        run: dir
      - name: listar caminho
        run: cd
      - name: conda version
        run: conda --version